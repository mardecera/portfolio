---
interface Props {
	className?: string
}

const { className } = Astro.props
---

<div class={`max-container ${className ?? ""}`}>
	<slot />
</div>

<style>
	.max-container {
		max-width: 1080px;
		margin: 0 auto;
		padding-inline: 2.5rem;
	}
</style>
